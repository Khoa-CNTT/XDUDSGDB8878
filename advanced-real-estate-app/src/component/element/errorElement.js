export const elements = {
    forAuctionWinDetailModal: {
        errorMessage: (errorMessage)=>{
            return(
                <span style={{color: 'red'}}>
                    {errorMessage}
                </span>
            );
        }
    },
}